import { FilterObjPipe } from './filter-obj.pipe';

describe('FilterObjPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterObjPipe();
    expect(pipe).toBeTruthy();
  });
});
